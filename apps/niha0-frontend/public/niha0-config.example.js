# Optional runtime override when the SPA talks to Render without the Pages /api proxy.
# Copy to niha0-config.js and set apiUrl (include /api suffix).
window.__NIHA0_CONFIG__ = {
  // apiUrl: "https://niha0-api.onrender.com/api",
};
