-- Phase B placeholder: demo seed accounts remain in schema for local/dev.
-- Production deactivates them at startup via DemoUserDeactivator (@Profile("prod")).
-- Login is also rejected when DEMO_LOGIN_ENABLED=false.

SELECT 1;
